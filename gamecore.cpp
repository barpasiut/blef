#include "gamecore.h"

GameCore::GameCore()
{

}

void GameCore::playerTurn(const QStringList &c, int n, int n2, int n3)
{
    turns++;
    if(turns % nrOfPlayers == 1)
        return;
    if(n2 < 9\)
    {
        for(int i=0; i<c.size(); i++)
        {
            if()
        }
    }
}
